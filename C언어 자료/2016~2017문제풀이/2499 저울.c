#include <stdio.h>
#include <stdlib.h>
int f();

int main(int argc, char *argv[]) {
	int i, j, t;
	int a[1000]={0,};
	scanf("%d", &t);
	for(i=0;i<t;i++)
	{
		scanf("%d", a[i]);
	} 
	return 0;
}
int f(int x)
{
	
}
